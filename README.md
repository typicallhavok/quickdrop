# Quickdrop

A peer-to-peer file transfer application.

## Project Structure

- `frontend/`: The graphical user interface built with Tauri, SvelteKit, and Rust.
- `backend/`: The core P2P networking and file transfer engine written in Rust.
